using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This is an empty script but is used to count
/// How many boxes there are within the map
/// The game manager simply looks for this script and adds on to the total boxes.
/// </summary>
public class BoxCount : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
